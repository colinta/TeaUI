{
  "name": "{{name}}",
  "main": ".dist/index.js",
  "license": "",
  "scripts": {
    "build": "$npm_execpath tsc",
    "start": "$npm_execpath build && node --enable-source-maps -- .dist/index.js",
    "clean": "rm -rf .dist/"
  },
  "private": true,
  "dependencies": {
    "preact": "^10.24.3",
    "@teaui/core": "latest",
    "@teaui/preact": "latest"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
